ANTHROPIC_API_KEY=sk-ant-...
PORT=5177

# Required before deploying publicly (see server.py) — leave unset for local dev.
APP_PASSWORD=

# Auto-injected by Vercel when you connect the Upstash Redis integration.
# Leave unset locally — the app falls back to state.json on disk.
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
